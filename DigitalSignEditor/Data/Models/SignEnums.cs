namespace DigitalSignEditor.Data.Models {

    public enum CollegeType { College_of_Education, Gies_College_of_Business, College_of_Education_CRC, Swanlund }

    public enum SignType { Lobby, Simple, SimpleWithTitle, Convocation }

    public enum SlideType { Image, Weather, Video, Calendar }
}